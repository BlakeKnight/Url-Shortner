<p id="notice"><%= notice %></p>

<p>
  <strong>Long url:</strong>
  <%= @url_mapper.long_url %>
</p>

<p>
  <strong>Short url:</strong>
  <%= link_to "#{@hostpath}/#{@url_mapper.id}", "#{@hostpath}/#{@url_mapper.id}" %>
</p>

<%= link_to 'Edit', edit_url_mapper_path(@url_mapper) %> |
<%= link_to 'Back', url_mappers_path %>
